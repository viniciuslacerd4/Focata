import Testing

@testable import Focata

/// Placeholder da Fase 0: garante que o alvo de testes compila e enxerga o
/// módulo do app. Os testes de verdade (PomodoroEngine, HistoryStore) chegam
/// nas Fases 2 e 3.
@Test func moduloDoAppCarrega() {
    #expect(Bool(true))
}
